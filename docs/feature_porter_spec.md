# Spec: Feature Porter CLI Tool (fs)
#
# Requirements:
# 1. Provide a global CLI command `fs`.
# 2. Provide an interactive selection menu of features:
#    - gitflow_sync (gf)
#    - docgen_sync (dg)
#    - testgen_sync (tg)
#    - gitlint_sync (glnt/cm)
#    - release_sync (rl)
# 3. Explain each feature and guide the user: "What to do next" (e.g. commands to run, hooks to check).
# 4. Copy configurations / hooks / CI templates to the current workspace root.
